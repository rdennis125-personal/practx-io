name: Build Service API

on:
  push:
    paths:
      - 'practx-service-api/**'
      - 'practx-shared-dotnet/**'
      - 'infra/modules/service-api/**'
      - 'github/workflows/build-service-api.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restore
        run: dotnet restore practx-service-api/src/Practx.Service.Api/Practx.Service.Api.csproj
      - name: Build
        run: dotnet build practx-service-api/src/Practx.Service.Api/Practx.Service.Api.csproj --configuration Release --no-restore
      - name: Test
        run: dotnet test practx-service-api/tests/Practx.Service.Api.Tests/Practx.Service.Api.Tests.csproj --configuration Release --no-build
      - name: Install Swashbuckle CLI
        run: dotnet tool install --global Swashbuckle.AspNetCore.Cli --version 6.5.0
      - name: Export OpenAPI
        run: |
          ~/.dotnet/tools/dotnet-swagger tofile \
            --output practx-service-api/OpenAPI/openapi.yaml \
            practx-service-api/src/Practx.Service.Api/bin/Release/net8.0/Practx.Service.Api.dll \
            v1
      - name: Upload OpenAPI artifact
        uses: actions/upload-artifact@v4
        with:
          name: service-api-openapi
          path: practx-service-api/OpenAPI/openapi.yaml
